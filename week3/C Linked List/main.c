#include <stdio.h>
#include "list.h"


int main(void)
{
	struct List list;
	init(&list);

	/* 
	 * main.c is not included int the grading,
	 * so, feel free to write your own test code here.
	 */
	return 0;
}

